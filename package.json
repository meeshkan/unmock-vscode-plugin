{
	"name": "unmock",
	"publisher": "Unmock",
	"displayName": "unmock-vs",
	"description": "Unmock plugin for VSCode",
	"license": "MIT",
	"version": "0.0.1",
	"engines": {
		"vscode": "^1.32.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [
		"workspaceContains:**/*.json"
	],
	"main": "./out/extension.js",
	"contributes": {
		"configuration": [
			{
				"title": "Unmock Configuration",
				"type": "object",
				"properties": {
					"unmock.refreshToken": {
						"type": [
							"string",
							"null"
						],
						"default": null,
						"description": "The refresh token to get when you registered for unmock. Used for various operations with the remote unmock service."
					},
					"unmock.path": {
						"type": "string",
						"default": ".unmock",
						"description": "Default path to look for stored credentials (relative to the workspace ror absolute path)."
					},
					"unmock.mocksPath": {
						"type": "string",
						"default": ".unmock/save",
						"description": "Default path to look for mock files (relative to workspace or absolute path)"
					}
				}
			}
		],
		"commands": [],
		"viewsContainers": {
			"activitybar": [
				{
					"id": "unmock-activity-button",
					"title": "Unmock",
					"icon": "./resources/unmock-logo.bmp"
				}
			]
		},
		"views": {
			"unmock-activity-button": [
				{
					"id": "unmock.mocksExplorer",
					"name": "Mock Explorer"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "yarn run compile",
		"compile": "tsc -p ./",
		"watch": "tsc -watch -p ./",
		"postinstall": "node ./node_modules/vscode/bin/install",
		"test": "yarn run compile && node ./node_modules/vscode/bin/test"
	},
	"dependencies": {
		"@types/ini": "^1.3.30",
		"axios": "^0.18.0",
		"ini": "^1.3.5"
	},
	"devDependencies": {
		"@types/mocha": "^2.2.42",
		"@types/node": "^10.12.21",
		"tslint": "^5.12.1",
		"typescript": "^3.3.1",
		"vscode": "^1.1.28"
	}
}
